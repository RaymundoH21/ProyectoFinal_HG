@model ProyectoFinal_HG.Models.Categoria

@{
    ViewBag.Title = "Eliminar categoria";
}
<hr />
<hr />  
<h2 class="mt-4"> Eliminar categoria </h2>

<h3>Estas seguro de que quieres eliminar esta categoria?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <input type="submit" value="Eliminar" class="btn btn-primary bg-primario" />
    <a href="@Url.Action("Index", "Categorias", new { id = Model.Id})" title="Regresar a la lista" class="btn btn-secondary bg-primario">regresar a la lista</a>
</div>
    }
</div>
